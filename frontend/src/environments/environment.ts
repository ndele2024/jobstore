/**
 * Configuration d'execution du frontend.
 * Modifier `apiBaseUrl` si l'API n'ecoute pas sur le port par defaut.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5081/api',
  appName: 'JobStore',
};
